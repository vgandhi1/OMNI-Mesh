name: 'mfg_mesh'
version: '0.1.0'
config-version: 2

profile: 'mfg_mesh'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  mfg_mesh:
    silver:
      +materialized: table
      +schema: silver
    gold:
      +materialized: table
      +schema: gold
