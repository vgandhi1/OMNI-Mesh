# =============================================================================
# RoboMesh — optional ML / VLA flywheel dependencies
# Install with:  pip install -r requirements-ml.txt
# Required for: PyTorch CV feature extraction, Ray Data streaming, WebDataset
#               shard writing, and the closed-loop policy logger.
# =============================================================================

# --- PyTorch (CPU wheels work fine for the demo; install GPU wheels for
# real training). torch is pinned ahead of torchvision so resolver picks
# compatible pairs.
torch>=2.2.0,<2.6
torchvision>=0.17.0,<0.21

# --- Tensor streaming -----------------------------------------------------
# WebDataset gives us pre-shuffled, sharded tarball outputs for high-throughput
# GPU training (mitigates the random-shuffle dilemma described in
# docs/RoboMesh.md Phase 2.5).
webdataset>=0.2.86

# --- Distributed Dataset orchestration (optional but powerful) -----------
# Ray Data sits between Iceberg and PyTorch DDP/FSDP, streaming Arrow buffers
# directly into GPU workers. Falls back to plain torch DataLoader if Ray is
# not installed.
ray[data]>=2.34.0
