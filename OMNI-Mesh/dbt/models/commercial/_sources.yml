version: 2

sources:
  - name: commercial_bronze
    description: Bronze subscription events exported from the Iceberg lakehouse.
    tables:
      - name: subscription_events
        meta:
          external_location: "read_parquet('{{ env_var('OMNI_MESH_BRONZE_PARQUET', '/tmp/omni_bronze.parquet') }}')"
